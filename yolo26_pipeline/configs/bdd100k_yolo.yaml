# BDD100K Dataset Configuration — Vehicle-Only Detection
# Used by: 02_bdd100k_preparation.ipynb, 03_bdd100k_training.ipynb

# Dataset root (set during preparation)
path: /content/bdd100k_yolo

# Train/val image directories (relative to path)
train: images/train
val: images/val

# Vehicle-only classes
nc: 5

names:
  0: car
  1: truck
  2: bus
  3: motorcycle
  4: bicycle
